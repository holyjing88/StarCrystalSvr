# IDIP Web Client 测试用例

> 实现：`tools/idip-webclient/src/tests/` · 执行：`bash tools/scripts/idip-webclient/run-regression.sh` 或 `IDIP_BASE_URL=… npm run regression`  
> 设计对照：[H5游戏发布与运营登录设计.md](./H5游戏发布与运营登录设计.md) §9  
> 服务端 IDIP：[../../../doc/IDIP_API.md](../../../doc/IDIP_API.md)

---

## 1. 自动化总览

| 套件 | 条数 | 环境变量 | 说明 |
|------|------|----------|------|
| `regression.test.ts` | 11 | `IDIP_BASE_URL`、`IDIP_KEY` | 金币/福利/任务（**Key 鉴权**，无需 login） |
| `idip_publish_regression.test.ts` | 16 | `IDIP_BASE_URL`、`IDIP_USERNAME`、`IDIP_PASSWORD`、`IDIP_KEY` | P0–P3 发布与登录（**Session**） |
| `webclient_deploy.test.ts` | 3 | `IDIP_WEBCLIENT_URL` | 静态页 + nginx 反代冒烟 |

可选：`IDIP_SKIP_H5=1` 跳过依赖服务器 `publish.h5AssetsDir` 可写目录的 H5 上传用例。

---

## 2. 运营登录与鉴权（P0）

| 用例 ID | 接口 / 场景 | 验证点 | Go 单测 | Vitest |
|---------|-------------|--------|---------|--------|
| **IDP-AUTH-001** | `POST /idip/v1/auth/login` 错误密码 | 401，`code=1401` | `TestIdipAuth_LoginWrongPassword` | ✓ |
| **IDP-AUTH-002** | login + `POST /auth/heartbeat` | `sessionToken`、心跳 200 | `TestIdipAuth_LoginWithEncryptedOperator` | ✓ |
| **IDP-AUTH-003** | logout 后 heartbeat | 401 / heartbeat false | — | ✓ |
| **IDP-AUTH-004** | 仅 `X-IDIP-Key` 访问 `GET /games/list` | 200，无 Session | `TestIdipAuth_KeyOnlyAccessProtectedRoute` | ✓ |
| **IDP-AUTH-005** | 无 Key 无 Session 访问 `games/list` | 403，`code=1403` | `TestIdipAuth_SessionRequiredWithoutKeyOrSession` | ✓ |
| **IDP-AUTH-006** | 同账号二次 login | 旧 token 心跳 401 | `TestIdipAuth_LoginKickInvalidatesFirstSession` | ✓ |
| **IDP-AUTH-007** | 5 次/分钟登录失败 | 429，`code=1429` | （限流集成，可选） | 规划 |
| **IDP-AUTH-008** | 心跳超时无请求 | 401 | — | 规划 |
| **IDP-AUTH-009** | 30s 心跳；31s 无心跳 | 401 | — | 规划 |

**密码配置**：`starcrystal.json` → `idip.operators[].passwordEnc`；生成脚本：

- Linux：`tools/idip-webclient/scripts_encrypt/encrypt-idip-operator.sh`（无参默认；自动写配置）
- Windows：`scripts_encrypt/encrypt-idip-operator.ps1`

**Go 加密**：`internal/service/idip_operator_crypto_test.go`、`cmd/encrypt-idip-operator/main_test.go`

---

## 3. 游戏列表与配置（P1）

| 用例 ID | 接口 | 验证点 | Go | Vitest |
|---------|------|--------|-----|--------|
| **GAMES-LIST-001** | `GET /idip/v1/games/list` | `configVersion` 为 64 位 SHA256 | `TestIdipGamesListAndUpsert` | ✓ |
| **GAMES-CONF-001** | `POST /games/upsert` 过期 version | 409，`code=1409` | 同上 | ✓ |
| **GAMES-UPSERT-001** | upsert + batch-upsert | `code=0`，version 递增 | 同上 | ✓ |
| **GAMES-DEL-001** | delete `online` 游戏 | 400，message 含 offline | `TestIdipGamesDeleteRequiresOffline` | ✓ |
| **GAMES-DEL-002** | delete offline，`deleteH5Dir=false` | 仅删 json | — | ✓（依赖 H5-UP） |
| **API-GAMES-001** | `GET /api/v1/games` 缺 `v=` | 跳过条目 + Error 日志 | `game_service_download_test` 等 | 规划 |

---

## 4. H5 上传（P1–P2）

| 用例 ID | 场景 | 验证点 | Go | Vitest |
|---------|------|--------|-----|--------|
| **H5-UP-011** | zip 名与顶层目录不一致 | 400 | `idip_games_upload_handler_test` | ✓ |
| **H5-UP-009** | offline 同 gameId+目录覆盖 | 200 | `TestIdipGamesH5UploadAndAudit` | ✓（环境可写时） |
| **H5-UP-006/007** | 目录 gameId 冲突 | 400 | `h5_upload_test` | 规划 |
| **H5-UP-010** | 写 json 失败回滚目录 | 500 + 无残留目录 | — | 规划 |

夹具：`src/tests/fixtures/vitest-game1.zip`（顶层目录 `vitest-game1`）。

---

## 5. 审计与发布（P2–P3）

| 用例 ID | 场景 | 验证点 | Go | Vitest |
|---------|------|--------|-----|--------|
| **AUDIT-001** | `GET /audit/logs` 分页 | `games_upsert` ≥1；**`action=login` 为 0**（设计：登录成功/失败均不写审计） | — | ✓ |
| **AUDIT-002** | upsert / h5 后查审计 | `games_upsert`；`h5_upload` 在上传成功时 | upload handler 单测 | ✓ |
| **AUDIT-003** | `login_kick` | 单端踢人时记一条 | auth handler | 规划 |
| **PUBLISH-RSYNC-001** | `POST /publish/rsync-retry` | 未实现时 skip（405/404） | — | ✓ skip |

当前审计为 **内存** `AuditRecorder`；MySQL `idip_audit_log` 为 P3。

---

## 6. 备份策略（P1）

| 用例 ID | 场景 | Go |
|---------|------|-----|
| **H5-SYNC-005** | `games.json.bak.*` 最多 10 份 | `backup_prune_test.go` |
| **H5-SYNC-004** | 覆盖上传前备份 | `games_config_store_test` / 集成 |

---

## 7. 存量经济/任务 IDIP（Key 回归）

见 § 自动化映射（`cases.ts`）：**IDP-002**～**IDP-010**、**TSK-***。  
服务端：`economy_smoke_test`、`idip_task_handlers_test`、`idip_economy_handlers_test`。

---

## 8. Linux 上线前验收脚本

`tools/scripts/_idip_linux_acceptance.py`（本机 SSH → 192.168.75.99）：

1. `go build` ELF + `release/startsvr.sh`（**不用 Windows exe**）
2. 写入 `passwordEnc` 配置
3. login / heartbeat curl
4. `go test`（service crypto、api IdipAuth、encrypt CLI）

---

## 9. 变更记录

| 日期 | 说明 |
|------|------|
| 2026-06-01 | 补齐 P0 登录/Key/单端、AUDIT 与设计对齐、encrypt.sh MERGE 修复、backup_prune 单测 |
