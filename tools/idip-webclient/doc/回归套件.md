# 三类回归套件对照

| 子 Tab | 目录定义 | 执行入口 |
|--------|----------|----------|
| IDIP 回归 | `src/tests/cases.ts` | 浏览器 / `npm run regression` |
| 服务端回归 | `src/regression/serverCatalog.ts` | dev → `POST /dev/regression/server` |
| 客户端回归 | `src/regression/clientCatalog.ts` | dev → `POST /dev/regression/client` |
| 运维脚本验收 | `server-go/tools/scripts/test/script-catalog.json` | `tools/scripts/test-scripts.ps1`（见 `server-go/doc/SCRIPTS_TESTING.md`） |

## 运维脚本（SCR-*）

在 `server-go/release` 下执行，覆盖 Windows / Linux 启停入口（`startdb` `stopdb` `startsvr` `startallsvr` 等）。静态检查默认通过；`-Live` / `SCRIPTS_TEST_LIVE=1` 做 Redis/MySQL/API 启停实测。

## 服务端（19 条 · 17 个 go test）

> 与 `doc/后台测试用例-v7.1.md` §I / §L 对齐；`TSK-S-003` 与 `TSK-S-004` 共用 `TestTasksClaim_DailyFree`。

| 用例 ID | go test |
|---------|---------|
| IDP-001 | `TestSmoke_IDIP_ForbiddenFromPublicIP` |
| TSK-S-006 | `TestIdipTaskTierPolicy_TogglesP1` |
| TSK-A-001 | `TestIdipTaskDefinitions_ReturnsCatalog` |
| TSK-A-002 | `TestIdipTaskDefinitionUpsert_Readback` |
| TSK-S-001 | `TestTasksClaim_NoBearer` |
| TSK-S-welfare | `TestTasksWelfare_GuestAuth` |
| TSK-S-002 | `TestTasksClaim_Play60s_NotClaimable` |
| TSK-S-004 / TSK-S-003 | `TestTasksClaim_DailyFree` |
| TSK-S-007 | `TestTasksClaim_DailyInvite_NotClaimable` |
| TSK-S-visit | `TestTasksReport_PageView` |
| TSK-S-play60 | `TestTaskService_Play60sProgress` |
| TSK-S-signin / TSK-S-free | `TestTaskService_Signin7dClaimable` / `DailyFreeClaimable` |
| TSK-catalog / TSK-tier | `TestBuildFullTaskCatalog_Counts` / `TestTaskTierPolicy_FiltersList` |
| RNK-S-activity | `TestReportActivityPlay_AccumulatesByAccountID` |
| FAV-S-001 | `TestGameFavorite_AddListRemove` |
| FAV-S-002 | `TestGameFavoriteService_AddListRemove` |

新增实现（2026-05）：`TestTasksClaim_NoBearer`、`TestTasksClaim_Play60s_NotClaimable`、`TestIdipTaskDefinitionUpsert_Readback`、`TestGameFavorite_AddListRemove` / `TestGameFavoriteService_AddListRemove`（游戏收藏）。

## 客户端（43 条）

覆盖 `客户端测试用例.md` 已自动化：**TSK**、**AUTH-I/P**、**RNK-I/P**、**ECO/WEL**、**CFG/SES**、**FAV**（§M）、**RNK-P-006** 等。Unity batch 跑全量 EditMode + PlayMode，按 `unityTest` 方法名映射。

收藏专项（无需 idip-webclient）：`StarCrystal2022/tools/run-favorite-client-tests.ps1`（HTTP）、`run-favorite-unity-tests.ps1`（Unity `-testFilter GameFavorite`）。

未纳入：`TSK-S-005` 断签 reconcile（服务端 ⬜）、纯 UI 手工项。
